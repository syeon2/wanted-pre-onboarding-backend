<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wanted.assignment.post.dao.mybatis.PostDetailMapper">

    <insert id="save">
        insert into post_detail (post_id, content, created_at, updated_at)
        values (#{data.postId}, #{data.content}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findById">
        select post_id, content
        from post_detail
        where post_id = #{postId}
    </select>

    <update id="update">
        update post_detail
        set content    = #{updateParam.data.content},
            updated_at = #{updateParam.updatedAt}
        where post_id = #{postId}
    </update>

    <delete id="delete">
        delete
        from post_detail
        where post_id = #{postId}
    </delete>

</mapper>
